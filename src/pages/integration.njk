---
layout: base.njk
title: Интеграция — DataLaw
---

{% from "ui/hero.njk" import hero %}
{% from "ui/section.njk" import section %}
{% from "ui/card.njk" import card %}
{% from "ui/button.njk" import button %}

{# --- HERO SECTION --- #}
{{ hero({
  eyebrow: "DataLaw API · Партнёрская программа",
  title: "Встройте инфраструктуру DataLaw<br>в <em>свои процессы</em>",
  description: "Мы предоставляем API-доступ к библиотеке прецедентов, AI-ассистенту Лайле и аналитическим инструментам.",
  bg: "alt"
}) }}

{# --- PARTNERSHIP FORMATS --- #}
{% call section({
  label: "Форматы",
  title: "Как мы работаем",
  class: "border-t"
}) %}
  <div class="grid grid-3 gap-8 mt-12">
    {{ card({
      label: "01",
      title: "API-Доступ",
      description: "Доступ к базе прецедентов и AI через REST API."
    }) }}
    {{ card({
      label: "02",
      title: "White-Label",
      description: "Развертывание CRM под вашим брендом."
    }) }}
    {{ card({
      label: "03",
      title: "Open Source",
      description: "Совместная разработка инструментов защиты."
    }) }}
  </div>
{% endcall %}

{# --- CTA BAND --- #}
{% call section({
  bg: "dark",
  class: "text-white"
}) %}
  <div class="flex justify-between items-center gap-8">
    <div>
      <p class="text-overline mb-2">DataLaw Integration</p>
      <h3 class="text-h2 text-white">Обсудим интеграцию</h3>
    </div>
    {{ button({ text: "Связаться с командой →", href: "/pages/contacts/" }) }}
  </div>
{% endcall %}
