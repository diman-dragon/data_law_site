---
layout: base.njk
title: Наша практика — DataLaw
---

{% from "ui/hero.njk" import hero %}
{% from "ui/section.njk" import section %}
{% from "ui/card.njk" import card %}
{% from "ui/button.njk" import button %}

{# --- HERO SECTION --- #}
{{ hero({
  eyebrow: "Case Studies · Стратегическая защита",
  title: "Практика <em>технологической</em><br>адвокации",
  description: "Мы используем семантический анализ, чтобы превращать факты в системные доказательства.",
  bg: "alt"
}) }}

{# --- CASE 001 --- #}
{% call section({
  label: "Кейс #001",
  title: "Отмена Red Notice",
  class: "border-t"
}) %}
  <div class="grid grid-2 gap-12 mt-12">
    <div>
      <p class="text-muted">Используя семантический поиск, мы доказали наличие стандартного паттерна преследования.</p>
    </div>
    {% call card({ title: "Результат" }) %}
      <p>Уведомление полностью удалено из баз Интерпола.</p>
    {% endcall %}
  </div>
{% endcall %}

{# --- CTA BAND --- #}
{% call section({
  bg: "base"
}) %}
  <div class="flex justify-between items-center gap-8">
    <h3 class="text-h2">Ваше дело требует анализа?</h3>
    {{ button({ text: "Связаться с командой", href: "/pages/contacts/" }) }}
  </div>
{% endcall %}
