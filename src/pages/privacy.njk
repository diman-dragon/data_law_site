---
layout: base.njk
title: Конфиденциальность — DataLaw
---

{% from "ui/hero.njk" import hero %}
{% from "ui/section.njk" import section %}
{% from "ui/card.njk" import card %}
{% from "ui/button.njk" import button %}

{# --- HERO SECTION --- #}
{{ hero({
  eyebrow: "Zero-Knowledge Policy",
  title: "Приватность через<br><em>архитектуру</em>",
  description: "Ваши данные не защищены нашим обещанием — они защищены математикой и кодом.",
  bg: "alt"
}) }}

{# --- LEGAL CONTENT --- #}
{% call section({
  class: "border-t"
}) %}
  <div class="grid grid-1 gap-12 mt-12">
    <div>
      <h3 class="text-h3 mb-4">Локальная обработка</h3>
      <p>Все чувствительные данные обрабатываются исключительно на вашем устройстве.</p>
    </div>
    <div>
      <h3 class="text-h3 mb-4">Zero-Knowledge Encryption</h3>
      <p>У разработчиков DataLaw нет ключей расшифровки.</p>
    </div>
    {% call card({ dark: true, title: "Адвокатская тайна" }) %}
      <p>Архитектура соответствует стандартам защиты профессиональной тайны.</p>
    {% endcall %}
  </div>
{% endcall %}

{# --- CTA BAND --- #}
{% call section({
  bg: "dark"
}) %}
  <div class="flex justify-between items-center gap-8">
    <h3 class="text-h2 text-white">Хотите проверить наш код?</h3>
    {{ button({ text: "GitHub Repository", href: "/pages/opensource/" }) }}
  </div>
{% endcall %}
